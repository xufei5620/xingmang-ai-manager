# --- Cloudflare-only access guard (origin protection) ---
# 放在 extension/ 目录：宝塔重写站点配置时不会清掉这里
set $block_noncf 0;
if ($from_cloudflare = 0) { set $block_noncf 1; }
if ($uri ~ "^/\.well-known/") { set $block_noncf 0; }
if ($block_noncf = 1) { return 444; }
