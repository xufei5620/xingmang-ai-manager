# 13 · 本轮修订与验证

本版：规格 v1.1、主原型 v2.1。保留原始压缩包及原版审查目录，修订只发生在新的独立原型包；应用仓库源码未修改。远端实际功能与本包未对齐事项见 `12-github-code-review.md`。

## 已修复

- 充值确认窗保留选择的金额与支付方式；返回页面也保留选择。这里仍是 mock，不代表真实下单。
- 中文组合输入的 Enter 不发送；聊天草稿按会话保存；停止使原请求失效，保留已有内容和草稿；切换会话和重试不串写旧结果。
- 页面/每层弹窗分别保留表单；子弹窗返回恢复工具页签及输入；保存某个工具后保留其他工具草稿；更换目录及时更新父窗。
- 有未保存输入时点遮罩保留表单；Esc/关闭明确选择放弃或继续；模态初始焦点、Tab循环、顶层交互、返回焦点和菜单键盘行为补齐。
- MCP 编辑修改原项目，不再新增重复项；更新失败的“重新下载”进入下载；修正“Gemini 必须依赖 Python”的原型表述，Python 保留为可选环境。
- 首页状态列最小86逻辑像素，工具名称列可收缩并提供完整内容提示，110%放大不再裁掉状态；最终缩放统一限制0.7–1.25。
- 提高深浅主题辅助文字可读性，修复浅色高对比映射；补字号/行高、间距、尺寸、圆角、层级、动效等共享Token。
- 基础组件目录由16扩为21，补Table、Tabs、Pagination、Callout、Tooltip；新增独立组件状态预览页 `prototype/components.html`。
- 统一基准优先级、主按钮作用域、弹窗规则、空态、取消/恢复状态、分期依赖和验收条件；回滚规定改为保留v2数据与快照，删除原来无保护的“删除v2数据”条款。
- 34张旧截图明确标为历史参考，新版截图单独存放。

## 主原型回归结果

使用独立 Chromium 浏览器、禁用 HTTP/HTTPS 网络访问，直接打开本地 HTML。没有启动 Electron 应用或操作实际账户/付款/安装。

原版针对已知问题的10组检查中，**1组通过、9组失败**。修订版使用相同检查并增加6组关联验证，最终 **16/16通过，页面脚本错误0**。

| 检查 | 结果 |
|---|---|
| 选择100及Creem后确认仍保持同一选择 | 通过 |
| isComposing Enter不发送 | 通过 |
| 停止后延迟结果不追加、下一条草稿保留 | 通过 |
| 草稿跨页面导航保留 | 通过 |
| 脏表单点遮罩不丢失 | 通过 |
| 子弹窗返回保留配置页签/Key/模型选择 | 通过 |
| Tab保持在弹窗内 | 通过 |
| MCP编辑不产生重复条目 | 通过 |
| 脏表单Esc后取消放弃，输入仍保留 | 通过 |
| 两个会话草稿隔离 | 通过 |
| 添加MCP默认聚焦名称输入 | 通过 |
| 子窗更换目录更新父窗只读显示 | 通过 |
| 保存当前工具，保留另一个工具页签草稿 | 通过 |
| 更新失败点击重新下载，进入下载状态 | 通过 |
| 布局/颜色矩阵 | 通过，见下 |
| 13主页面及8个个人中心页签基础渲染 | 通过 |

布局矩阵：Windows/macOS模拟 × 深/浅主题 × 1280×820、1093×574、960×560 × 自动/110%，共24种组合，工具状态标签未被状态列裁切。颜色检查覆盖深/浅×普通/高对比四种组合，辅助文字对不透明面板底的对比度均达到4.5:1。这不等于每一种文字/背景组合都已完成无障碍审核。

记录：`qa/original-checks.json`、`qa/revised-checks.json`、`qa/check-prototype.cjs`。新版截图索引为 `prototype/previews-v1.1/README.md`。截图已关闭动画并排除临时更新提醒，便于对照稳定状态。

组件预览页另完成 **9/9组检查，脚本错误0**：五种展示状态下21个组件均渲染、主题/高对比切换、弹窗初始焦点与Tab圈定/关闭、页签方向键和960宽布局。它是典型状态样例，不是每种组件所有业务行为的完整测试。记录为 `qa/components-checks.json`，检查脚本为 `qa/check-components.cjs`。

## 验证边界

主原型仍有未对齐实际产品的业务演示：注册/初始化、官方来源与共享配置、完整Key/看板/支付/订阅、多账号和部分系统设置。状态详见12，不应把“16组通过”解释成全部业务可发布。

中文输入验证使用组合输入事件，不替代真实输入法/操作系统测试。macOS/Windows标题栏是浏览器模拟；托盘、多屏、原生窗口、支付、服务端、安装更新和数据迁移未实测。本轮也未提交PR、推送、打包真实桌面安装器或发布应用。
