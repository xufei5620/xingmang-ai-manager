## 用户

- 「帮助与客服」里多了一行「找客服时把这行一起发过去」，写着你的账号、账号 ID、软件版本和系统，点「复制」就能直接发给客服，不用再一问一答。

## 开发

- 帮助弹窗加身份条（第十三批 6 的弹窗部分）：`src/renderer-v2/features/app/SupportIdentity.tsx` 纯函数拼「账号名（ID）· 版本 · 系统」，未登录写「未登录」，拿不到正整数 ID 时只写账号名；不带邮箱与站点名。反馈报告不变。
