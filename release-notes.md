0.1.12

- 修复 Codex Desktop 国内镜像迁移后无法下载、安装最新版的问题。
- 适配中科院高能所对象云的 AWS V4 签名跳转，只允许固定域名、固定存储桶路径和完整签名参数。
- 保留镜像清单、文件大小、SHA-256、MSIX 产品身份、版本、架构和签名校验，下载内容不符合官方包身份时仍会拒绝安装。
- API Key 输入框的小眼睛现在可以显示本机配置中已保存的完整 Key；普通配置扫描仍只返回掩码，查看动作不会误触发保存状态。
