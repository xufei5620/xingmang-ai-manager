## 用户

- 安装 Codex 桌面端时，如果换了一路下载，换的原因会在整个下载过程中一直显示，不再一开始下载就看不到了。

## 开发

- `codex-desktop-service.ts`：新增纯函数 `describeCodexDesktopDownloadAttempt`，每一路下载尝试开始时算好提示语并记住，`onProgress` 沿用它。此前 #238 加的「国内镜像本次不可用（…）」只在 0% 那条进度里出现，进度一动就被「正在从镜像备用源下载」盖掉。下载源顺序、加速与校验逻辑一行未动。
